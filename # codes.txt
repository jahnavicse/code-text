# codes 

1. // Online C# Editor for free
// Write, Edit and Run your C# code using C# Online Compiler

using System;

public class HelloWorld
{
    public static void Main(string[] args)
    {
        char letter = 'A';
        Console.WriteLine (letter);
    }
}

2. // Online C# Editor for free
// Write, Edit and Run your C# code using C# Online Compiler

using System;

public class Test
{
    public static void Main(string[] args)
    {
        Console.Write("Enter a number: ");
        int number = int.Parse(Console.ReadLine());
        Console.WriteLine(number);
    }
}

3.Online C# Editor for free
// Write, Edit and Run your C# code using C# Online Compiler

using System;

public class Test
{
    public static void Main(string[] args)
    {
         const double pi = 3.14;
         Console.WriteLine(pi);
    }
}

4. using System;

public class Test
{
    public static void Main(string[] args)
    {
         string s ="1234";
         Console.WriteLine(int.TryParse(s,out int result ));
         
    }
}

5.// Online C# Editor for free
// Write, Edit and Run your C# code using C# Online Compiler

using System;

    public class Person{
        public string Name;
    }
    public static void Main(string[] args)
    {
     Person p1 = new Person();
     Person p2 = p1;
     p2.Name ="john";
         
    }
6.// Online C# Editor for free
// Write, Edit and Run your C# code using C# Online Compiler

using System;

public class HelloWorld
{
    public static void Main(string[] args)
    {
        decimal price =90.00m;
       Console.WriteLine(price);
    }
}
7.// Online C# Editor for free
// Write, Edit and Run your C# code using C# Online Compiler

using System;

public class HelloWorld
{
    public static void Main(string[] args)
    {
        int? age = null;
        if (age.HasValue){
            Console.WriteLine(age.Value);
        }
    }
}
8.// Online C# Editor for free
// Write, Edit and Run your C# code using C# Online Compiler

using System;

public class HelloWorld
{
    public static void Main(string[] args)
    {
        int [] numbers =  new int [5] {1,2,3,4,5};
        Console.WriteLine(numbers[0]);
        Console.WriteLine(numbers[4]);
    }
}
9.// Online C# Editor for free
// Write, Edit and Run your C# code using C# Online Compiler

using System;

public class HelloWorld
{
    public static void Main(string[] args)
    {
        int [] numbers =  new int [6] {1,2,3,4,5,6};
        Console.WriteLine(numbers.Length);
    }
}
10.// Online C# Editor for free
// Write, Edit and Run your C# code using C# Online Compiler

using System;

public class HelloWorld
{
    public static void Main(string[] args)
    {
        int [] numbers =  new int [6] {1,2,3,4,5,6};
        numbers[2] =100;
        Console.WriteLine(numbers[2]);
       
    }
}
11.using System;

public class Test
{
    public static void Main(string[] args)
    {
        // -------------------------------
        // Multidimensional Array (2x2 Matrix)
        // -------------------------------
        int[,] multi = new int[2, 2]
        {
            {1, 2},
            {3, 4}
        };

        Console.WriteLine("Multidimensional Array:");

        for (int i = 0; i < multi.GetLength(0); i++)
        {
            for (int j = 0; j < multi.GetLength(1); j++)
            {
                Console.Write(multi[i, j] + " ");
            }
            Console.WriteLine();
        }

        // -------------------------------
        // Jagged Array (Array of Arrays)
        // -------------------------------
        int[][] jagged = new int[2][];

        // Initialize each inner array
        jagged[0] = new int[] { 10, 20 };
        jagged[1] = new int[] { 30, 40, 50 };

        Console.WriteLine("\nJagged Array:");

        for (int i = 0; i < jagged.Length; i++)
        {
            for (int j = 0; j < jagged[i].Length; j++)
            {
                Console.Write(jagged[i][j] + " ");
            }
            Console.WriteLine();
        }
    }
}
12.// Online C# Editor for free
// Write, Edit and Run your C# code using C# Online Compiler

using System;

public class HelloWorld
{
    public static void Main(string[] args)
    {
        int[] numbers = new int[5] {1,6,3,4,5};
        Array.Sort(numbers);
    }
}
13.// Online C# Editor for free
// Write, Edit and Run your C# code using C# Online Compiler

using System;

public class HelloWorld
{
    public static void Main(string[] args)
    {
        int[] numbers = { 10, 20, 30, 40, 50 };

        // Get last element using ^ operator
        int last = numbers[^1];
        Console.WriteLine("Last element: " + last);

        // Get slice using range operator
        int[] slice = numbers[1..3];

        Console.WriteLine("Slice (index 1 to 2) :" + slice);

    }
}
14.// Online C# Editor for free
// Write, Edit and Run your C# code using C# Online Compiler

using System;

public class HelloWorld
{
    public static void Main(string[] args)
    {
        int[] numbers = { 10, 20, 30, 40, 50 };

        Span<int> span = numbers;
        for( int i=0 ;i<span.Length;i++){
            Console.WriteLine(span[i] + " ");
        }
      
    }
}
15.// Online C# Editor for free
// Write, Edit and Run your C# code using C# Online Compiler

using System;

public class HelloWorld
{
    public static void Main(string[] args)
    {
       int[] numbers = { 10, 20, 30, 40, 50 };
    //   for (int i=0;i<numbers.Length ; i++){
    //       Console.WriteLine(numbers[i] + " ");
    //   }
    
    foreach( int number in numbers){
        Console.WriteLine (number + " ");
    }
      
    }
}
